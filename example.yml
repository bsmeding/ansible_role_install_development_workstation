---
# Example playbook for development/testing purposes
# End users should create their own playbook after installing via:
# ansible-galaxy role install bsmeding.install_development_workstation

- name: Setup Development Workstation
  hosts: localhost
  connection: local
  gather_facts: yes

  # The role defaults are minimal; load your package lists and preferences from a vars file:
  # vars_files:
  #   - vars/workstation.yml

  roles:
    - bsmeding.install_development_workstation
